# 📄 [PRD] arti-ops v0.5.0 제품 요구사항 명세서 (전면 개정판)

## 1. 제품 개요 (Product Overview)
* **제품명:** arti-ops (로컬 컨텍스트 기반 AgentOps TUI 플랫폼)
* **목적:** 사용자가 현재 작업 중인 경로(디렉토리)에서 명령어를 실행하면, **사내 위키(BookStack)의 정책과 현재 로컬에 작성된 파일들(현황)을 종합적으로 분석**하여, 사용자의 프롬프트 지시에 따라 대상 AI 에이전트(기본: antigravity)에 최적화된 스킬(Skill)과 룰(Rule)을 자동 생성 및 검증하는 전체화면 대화형 TUI 환경.
* **핵심 가치:** 단순한 정책 다운로드 도구를 넘어, 로컬 코드 문맥을 이해하고 회사 정책을 융합하여 개발자 맞춤형 AI 스킬/룰을 창작(Generation)하고 안전하게 배포하는 지능형 어시스턴트.

## 2. 핵심 기능 요구사항 (Functional Requirements)

* **F1. 로컬 컨텍스트 및 프롬프트 기반 자동 생성:**
  - 사용자는 하단 프롬프트 입력창을 통해 자연어로 지시를 내린다.
  - 시스템은 사내 위키(BookStack)의 Global/Workspace 정책뿐만 아니라, **명령어가 실행된 현재 경로의 로컬 파일 및 기존 산출물들을 스캔하여 컨텍스트로 참조**한다.
  - 이 두 가지 지식을 융합하여 타겟 에이전트(`antigravity`)가 이해할 수 있는 규격(`.agents/skills/`, `.agents/rules/`)으로 코드를 창작한다.
* **F2. 자율 정책 검증 (Policy Verification):**
  - 생성된 스킬과 룰이 회사의 L1(전역) 보안 정책이나 가이드라인을 위반하지 않는지 철저히 검증한다.
* **F3. 사전 검토 보고서 제출 및 반영 판단 (Pre-deployment Review):**
  - 검증이 완료되더라도 즉시 로컬 파일로 저장하지 않는다.
  - 생성된 파일들의 **정확한 목록과 전체 코드 내용, 검증 결과가 상세히 정리된 [최종 보고서]**를 화면 중앙에 출력한다.
  - 사용자는 이 보고서를 읽고 하단 프롬프트를 통해 승인(반영) 여부를 판단하거나, 추가 수정 지시(재생성 요구)를 내릴 수 있다.
* **F4. 전체화면 대화형 TUI (Full-screen Console):**
  - 콘솔의 전체화면(Full-screen)으로 동작한다.
  - 화면 상단/중앙은 파이프라인 진행 상태와 보고서가 출력되는 뷰어 영역이다.
  - 화면 하단은 사용자의 프롬프트와 단축키 명령어 힌트가 상시 나열되는 입력창이다.
* **F5. 즉시 종료 스위치 (Kill-switch):**
  - `q` (quit) 문자 입력 또는 `Ctrl+C` 키 입력 시, 파이프라인이 어떤 작업을 하고 있든 관계없이 무조건 즉시 안전하게 종료(Exit)되어야 한다.
* **F6. GWS 채팅 알림 간소화:**
  - 기존의 중간 결재/대기용 GWS Chat 연동 로직은 폐기한다.
  - 오직 로컬 배포가 사용자의 승인 하에 성공적으로 완료된 직후, **최종 요약 보고서**만을 GWS Chat으로 단방향 알림 전송한다.
